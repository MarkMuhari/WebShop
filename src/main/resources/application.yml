spring:
    data:
        rest:
            base-path: /api
    datasource:
        driver-class-name: com.mysql.cj.jdbc.Driver
        password: webshopapp
        url: jdbc:mysql://localhost:3306/full-stack-webshop?useSSL=false&useUnicode=yes&characterEncoding=UTF-8&allowPublicKeyRetrieval=true&serverTimezone=UTC
        username: webshopapp
    jpa:
        properties:
            hibernate:
                dialect: org.hibernate.dialect.MySQL8Dialect
